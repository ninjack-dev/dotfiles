#!/bin/sh
# The following is a wrapper for PowerShell because Microsoft apparently dislikes the idea of configuring things in a configuration file.
POWERSHELL_UPDATECHECK=Off exec "$(whereis pwsh | awk '{print $NF}')" -nologo "$@"
