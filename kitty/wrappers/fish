#!/bin/sh
quote() {
    printf "'"
    printf '%s' "$1" | sed "s/'/'\\\\''/g"
    printf "'"
}
_SHELL="$(whereis fish | awk 'NR == 1 { printf "%s", $4 }')"
[ -z "$_SHELL" ] && { printf 'Fish not found in PATH\n' 2>&1; exit 1; }
OUT=""
for a in "$@"; do
    OUT="$OUT $(quote "$a")"
done
exec kitten run-shell --shell="$_SHELL$OUT"
